[build]
  base    = "dashboard"
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# To wire the deployed dashboard to a real Node-RED instance,
# set this in Netlify -> Site settings -> Environment variables, then redeploy:
# NEXT_PUBLIC_WS_URL = "wss://your-noderef.example.com/ws/tanks"
